/**
 * Inbox Page
 * Unified multi-channel inbox for cross-platform conversations
 */

import UnifiedInbox from '../components/UnifiedInbox';

const Inbox = () => {
  return (
    <div className="h-full">
      <UnifiedInbox />
    </div>
  );
};

export default Inbox;
